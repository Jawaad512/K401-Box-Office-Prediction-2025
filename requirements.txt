# Pinned to the environment the committed notebook run was produced in.
# Python 3.12.10 on Windows.
#
# pandas 3.x is NOT supported: the cleaning cells rely on pre-copy-on-write
# assignment semantics and will silently no-op under 3.x.
#
# LightGBM 4.7 is incompatible with numpy < 2 (segfaults in LGBM_DatasetSetField),
# so numpy and lightgbm must be upgraded together.

numpy==2.5.3
pandas==2.3.3
scipy==1.18.1
scikit-learn==1.9.1
statsmodels==0.15.0
matplotlib==3.11.2
seaborn==0.13.2
tqdm==4.70.1
requests==2.34.2

catboost==1.2.10
xgboost==3.4.1
lightgbm==4.7.0

# Needed only to execute the notebook
ipykernel==7.3.0
